{
  "name": "pi-footer-composer",
  "version": "0.2.0",
  "description": "Compose the pi footer one content per line: environment, usage, context, model, then extension statuses each get their own row; wide groups wrap within the terminal width.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
