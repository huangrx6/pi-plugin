{
  "name": "pi-footer-composer",
  "version": "0.1.0",
  "description": "Compose the pi footer as a table: environment, usage, context, model, and every extension status each get their own cell; rows wrap to terminal width.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
