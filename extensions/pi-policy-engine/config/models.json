{
  "$comment": "Model policy routing (v0.22): structured matching — provider must match exactly (case-insensitive); model, when declared, must match exactly or prefix-match a trailing '*'. Legacy substring {\"match\": [...]} arrays are rejected. The v0.21 substring rules loaded MiniMax-M30 with the M3 policy and matched 'notdeepseek' as deepseek.",
  "rules": [
    { "provider": "minimax-cn", "model": "MiniMax-M3", "policy": "model.minimax-m3" },
    { "provider": "deepseek", "policy": "model.deepseek" }
  ]
}
