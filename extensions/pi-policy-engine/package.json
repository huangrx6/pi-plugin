{
  "name": "pi-policy-engine",
  "version": "0.28.0",
  "description": "Context-aware task policies for Pi with active-agent or dedicated-model intent recognition, persistent requirements and versioned plan approvals.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "policy",
    "plan-first",
    "agent"
  ],
  "pi": {
    "extensions": [
      "./extensions/policy-engine/index.js"
    ]
  },
  "scripts": {
    "test": "node --test && node ./scripts/smoke-extension.mjs",
    "check": "set -e; for f in extensions/policy-engine/*.js src/core/*.js scripts/*.mjs; do node --check \"$f\"; done"
  }
}
