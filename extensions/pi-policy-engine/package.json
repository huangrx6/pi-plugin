{
  "name": "pi-policy-engine",
  "version": "0.17.0",
  "description": "Task-flow policy router for Pi coding agent: routes prompts to quick/standard/strict workflows and injects execution-discipline constraints into the system prompt.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "policy",
    "plan-first",
    "agent"
  ],
  "pi": {
    "extensions": [
      "./extensions/policy-engine/index.js"
    ]
  },
  "scripts": {
    "test": "node --test && node ./scripts/smoke-extension.mjs",
    "check": "for f in extensions/policy-engine/*.js src/core/*.js; do node --check \"$f\"; done"
  }
}
