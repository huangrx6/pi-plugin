# Standard Workflow

Before mutation, form a compact Task Contract containing:

- Goal
- Confirmed facts
- MUST constraints
- MUST NOT constraints
- Verification criteria

Then inspect the relevant implementation, create a minimal implementation plan, execute it without unnecessary ceremony, and verify against the Task Contract.

Do not expand into unrelated architecture work unless required by evidence.
