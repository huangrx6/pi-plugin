# Quick Workflow

Use a lightweight flow, sized to the execution intent:

- For mutation tasks: Inspect -> Change -> Verify.
- For read-only tasks: Inspect -> Analyze/Report -> Validate conclusions.

Do not produce a formal plan unless needed to resolve ambiguity. Keep the work narrowly scoped and finish with direct verification (of the change, or of the conclusions).
