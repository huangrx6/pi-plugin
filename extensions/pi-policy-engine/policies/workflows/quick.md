# Quick Workflow

Use a lightweight flow:

Inspect -> Change -> Verify.

Do not produce a formal plan unless needed to resolve ambiguity. Keep the change narrowly scoped and finish with direct verification.
