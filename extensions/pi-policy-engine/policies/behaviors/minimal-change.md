# Minimal Change

Prefer the smallest correct change.

Do not refactor unrelated code, rename unrelated symbols, reorganize files, introduce abstractions without a concrete need, or fix adjacent issues unless they are required for the requested outcome.

If scope must expand, explain the dependency before expanding it.
