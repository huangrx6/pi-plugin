# Database Domain

Treat schema, data migration, transaction boundaries, compatibility, rollback, and data correctness as explicit concerns.

For migration work, identify the source of truth, cutover direction, rollback boundary, idempotency/replay behavior, and verification gates before mutation.
