# Kubernetes Domain

Reason about the effective runtime state, not YAML in isolation. Check mount shadowing, rollout behavior, probes, persistence, namespace/context, and rollback implications.

Avoid assuming a manifest change is safe merely because it validates syntactically.
