# 0.27–0.32 升级任务

目标：让识别理解当前任务与新消息的关系，让状态转换和授权具有可检查的依据。

- [x] T1：增加大模型优先识别，输入当前目标、约束、最近要求和阶段；返回经过校验的结构化意图与任务关系。兼容 OpenAI Chat Completions 与 Anthropic Messages 协议及本地无密钥服务。
- [x] T2：统一任务关系判定；续作保留任务 ID、风险背景和授权；新任务清空旧授权；模型不可自行授予执行权限。
- [x] T3：保存完整初始目标、用户要求原文、约束来源和计划版本；修订与初始要求均进入账本，预览不修改账本。
- [x] T4：用绑定任务 ID / 版本的结构化计划进入待审批；缺少计划、询问文件路径、失败回复不能充当计划；补充明确的审批与任务切换命令。
- [x] T5：在状态、预览与历史中显示识别来源、故障降级原因和上下文；校正配置档描述，避免暗示切换任务或授权。
- [x] T6：增加跨轮回归、模型协议与故障测试，更新配置示例、设计与变更记录。

后续评估（需要真实模型调用与实际任务样本，不能以 mock 测试代替）：比较识别正确率、任务误切换、约束丢失、无效审批等待、延迟及调用成本，再决定默认开启模型识别与策略正文压缩幅度。

交付范围：0.33.0 仓库实现与配置示例；未自动安装全局副本。模型预识别的真实效果仍需用实际任务样本评估。

验证结果：265 项测试通过，smoke、全部源文件语法检查、Markdown 检查、未使用导入/变量检查、版本与 manifest 引用检查通过。

- [x] T7：复用当前 agent 的模型与宿主认证，支持主模型切换和隔离预览；无需额外模型配置。
- [x] T8：把日常操作收敛为带注释的一级选择面板；高级诊断保留为参数型文本命令，不再占用日常菜单。
- [x] T9：把 `/policy` 收敛为一级面板，选择后立即保存，并显示真实个人配置位置。
- [x] T10：改为当前模型前置结构化意图识别；增加识别加载状态、结果状态和失败阻断提示，再按识别结果加载策略。
- [x] T11：把预识别迁移到 Pi 的 `context` 生命周期；让用户消息先显示，并使用宿主 Working 行显示“意图识别中…”，在 `before_provider_request` 注入最终策略。
- [x] T12：区分中断任务的显式续接与新任务；续接仍由当前模型识别，并在识别结果确认策略未变化时复用策略块和活动指纹，每次实际 provider 请求仍保持幂等注入。
- [x] T13：删除旧配置/状态回读、规则语义 fallback、一次性/profile/save/直接模式命令；统一使用 `recognition` 配置键和一级控制面板，在线识别失败直接阻断。
